server:
   port:8888
logging:
    path:logs
    file:mylog.log

spring:
  application:
    name:myTest
  datasource:
    driver-class-name:com.mysql.jdbc.Driver
    url:jdbc:mysql://loclhost:3306/course
    username:root
    password:yjx2013

mybatis:
  type-aliases-package:com.course.model
  mapper-locations:
    - mapper/*

